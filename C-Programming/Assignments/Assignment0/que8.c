// Write a C program to input the length and width of a rectangle and find its perimeter.

#include<stdio.h>
void main(){
    int length = 10;
    int width = 5;
    int perimeter =  2 * (length + width);
    printf("The perimter of the Reactangle = %d ", perimeter);

}